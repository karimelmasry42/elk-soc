# Mini SOC — Beats input.
# Filebeat, Packetbeat, and Winlogbeat all ship to 5044 over plain TCP (Lumberjack).
# Lab only: no TLS, no client auth.

input {
  beats {
    port => 5044
    host => "0.0.0.0"
  }
}
